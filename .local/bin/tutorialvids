#!/bin/sh

# Tutorial video selector - customize with your own tutorial links
# Format: "Title	URL"

vidlist="
# Add your own tutorial videos here in format:
# Description	URL
"

# Uncomment and populate vidlist, then use:
# echo "$vidlist" | grep -P "^$(echo "$vidlist" | grep "https:" | sed 's/\t.*//g' | dmenu -i -p "Select tutorial:" -l 20 | awk '{print $1}')\s" | sed 's/.*\t//' | xargs -r mpv
